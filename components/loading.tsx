import { BoxLoading } from 'react-loadingg';

const Loading = () => {
    return (
        <div className="h-screen z-50 w-screen bg-white grid place-items-start fixed top-0  text-gray-700">
       
<BoxLoading />;

        </div>
    );
}

export default Loading;